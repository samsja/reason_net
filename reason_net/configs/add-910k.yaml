defaults:
  - module/model: 910K
  - default
  - _self_

data:
  batch_size: 8196
  num_workers: 4
  dataset_path: datasets/data-25m-add

module:
  lr: 1e-2

trainer:
  pl:
    val_check_interval: 3000 # this means two time per epoch

wandb:
  enabled: true
  project_name: "reason_net_add"